
preprocess:
  cmd: "cog --verbosity=1 -r %s"

build:
  cmd: "python -m dpbuild %s --discover ../libraries --dest %s"

filter:
  includeGlobs:
    - "**/*.mcfunction"
    - "**/*.json"
    - "**/*.cog"
    - "**/pack.mcmeta"
  excludeGlobs:
    - "./resourcepacks/**"