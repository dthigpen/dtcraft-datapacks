#> dt.array:internal/funct/base/args/push
# Base function that all overriding functions must call
# only pushes a new scope for now but could do more in the future
# @params
#   storage call_stack: call.arg0
#       array
function call_stack:push